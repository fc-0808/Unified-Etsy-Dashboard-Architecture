pdfplumber>=0.11.0
pymupdf>=1.24.0
openpyxl>=3.1.0
rapidfuzz>=3.0.0
# Thumbnails in the «Mark product discontinued» dialog (simple_ui.py).
Pillow>=10.0.0

# Optional: drag-and-drop onto the Charms tab in simple_ui.py (Windows).
windnd>=1.0.7; sys_platform == "win32"

# Optional: charm vision SKU uses OpenAI-compatible HTTP API (stdlib only); see CHARM_VISION_* / OPENAI_* env vars.
